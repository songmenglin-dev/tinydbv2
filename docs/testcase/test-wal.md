# WAL (Write-Ahead Logging) 测试用例

## 测试范围

测试 `src/storage/wal.c` 的预写日志功能。

## 测试用例

### TC-WAL-001: 创建 WAL 文件

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-001 |
| **标题** | 创建 WAL 文件 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `wal_open()` 创建新的 WAL 文件 |
| **预期结果** | WAL 文件成功创建 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-002: WAL_BEGIN_TX 标记

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-002 |
| **标题** | WAL_BEGIN_TX 标记 |
| **优先级** | P0 |
| **前置条件** | 已打开 WAL |
| **测试步骤** | 1. 调用 `wal_begin_tx()` <br> 2. 验证返回成功 |
| **预期结果** | 事务开始标记成功写入 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-003: WAL_COMMIT_TX 标记

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-003 |
| **标题** | WAL_COMMIT_TX 标记 |
| **优先级** | P0 |
| **前置条件** | 已开始事务 |
| **测试步骤** | 1. 调用 `wal_begin_tx()` <br> 2. 调用 `wal_commit_tx()` |
| **预期结果** | 事务提交标记成功写入 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-004: WAL_ROLLBACK_TX 标记

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-004 |
| **标题** | WAL_ROLLBACK_TX 标记 |
| **优先级** | P1 |
| **前置条件** | 已开始事务 |
| **测试步骤** | 1. 调用 `wal_begin_tx()` <br> 2. 调用 `wal_rollback_tx()` |
| **预期结果** | 事务回滚标记成功写入 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-005: WAL_WRITE_PAGE 页面写入

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-005 |
| **标题** | WAL_WRITE_PAGE 页面写入 |
| **优先级** | P0 |
| **前置条件** | 已开始事务 |
| **测试步骤** | 1. 调用 `wal_begin_tx()` <br> 2. 调用 `wal_write_page()` 写入页面数据 |
| **预期结果** | 页面修改记录成功写入 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-006: WAL_CHECKSUM 校验和计算

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-006 |
| **标题** | WAL_CHECKSUM 校验和计算 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `wal_checksum()` 计算数据校验和 |
| **预期结果** | 返回正确的 CRC32 校验和 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-007: WAL_CHECKPOINT 检查点

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-007 |
| **标题** | WAL_CHECKPOINT 检查点 |
| **优先级** | P1 |
| **前置条件** | 已写入多个事务 |
| **测试步骤** | 1. 写入多个事务 <br> 2. 调用 `wal_checkpoint()` |
| **预期结果** | 检查点标记成功写入 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-008: WAL_GET_PATH 路径生成

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-008 |
| **标题** | WAL_GET_PATH 路径生成 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `wal_get_path("test.db")` |
| **预期结果** | 返回 "test.db-wal" 路径 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-009: WAL_TRUNCATE 日志截断

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-009 |
| **标题** | WAL_TRUNCATE 日志截断 |
| **优先级** | P2 |
| **前置条件** | 已设置检查点 |
| **测试步骤** | 1. 执行检查点 <br> 2. 调用 `wal_truncate()` |
| **预期结果** | WAL 文件被截断 |
| **测试结果** | ✅ PASS |

---

### TC-WAL-010: 多事务日志写入

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-WAL-010 |
| **标题** | 多事务日志写入 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 执行多个事务 <br> 2. 验证每个事务日志正确 |
| **预期结果** | 所有事务日志正确写入 |
| **测试结果** | ✅ PASS |

---

## 测试结果汇总

| 测试用例 | 结果 | 备注 |
|---------|------|------|
| TC-WAL-001 | ✅ PASS | |
| TC-WAL-002 | ✅ PASS | |
| TC-WAL-003 | ✅ PASS | |
| TC-WAL-004 | ✅ PASS | |
| TC-WAL-005 | ✅ PASS | |
| TC-WAL-006 | ✅ PASS | |
| TC-WAL-007 | ✅ PASS | |
| TC-WAL-008 | ✅ PASS | |
| TC-WAL-009 | ✅ PASS | |
| TC-WAL-010 | ✅ PASS | |

**通过率: 10/10 (100%)**