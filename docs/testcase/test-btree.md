# B+Tree Storage 测试用例

## 测试范围

测试 `src/storage/btree.c` 的 B+Tree 存储引擎功能。

## 测试用例

### TC-BTREE-001: 创建 B+Tree

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-001 |
| **标题** | 创建 B+Tree |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `btree_create()` 创建新树 <br> 2. 关闭树 |
| **预期结果** | 树创建成功，无内存泄漏 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-002: 单键值插入

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-002 |
| **标题** | 单键值插入 |
| **优先级** | P0 |
| **前置条件** | 已创建 B+Tree |
| **测试步骤** | 1. 插入键值对 (key=1, value="test") <br> 2. 关闭树 |
| **预期结果** | 插入成功，btree_close 成功 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-003: 查找存在的键

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-003 |
| **标题** | 查找存在的键 |
| **优先级** | P0 |
| **前置条件** | 已插入键值对 (key=1, value="test") |
| **测试步骤** | 1. 调用 `btree_find(1)` |
| **预期结果** | 返回对应的值 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-004: 查找不存在的键

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-004 |
| **标题** | 查找不存在的键 |
| **优先级** | P0 |
| **前置条件** | 已插入键值对 (key=1, value="test") |
| **测试步骤** | 1. 调用 `btree_find(999)` |
| **预期结果** | 返回 NULL |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-005: 页面分裂

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-005 |
| **标题** | 页面分裂 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 插入多个键值对触发页面分裂 <br> 2. 验证所有键值对可找到 |
| **预期结果** | 页面正确分裂，数据不丢失 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-006: 删除存在的键

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-006 |
| **标题** | 删除存在的键 |
| **优先级** | P0 |
| **前置条件** | 已插入键值对 (key=1, value="test") |
| **测试步骤** | 1. 调用 `btree_delete(1)` <br> 2. 调用 `btree_find(1)` |
| **预期结果** | 删除成功，查找返回 NULL |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-007: 删除不存在的键

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-007 |
| **标题** | 删除不存在的键 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `btree_delete(999)` |
| **预期结果** | 删除操作成功（或返回错误但无崩溃） |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-008: 更新键值

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-008 |
| **标题** | 更新键值 |
| **优先级** | P0 |
| **前置条件** | 已插入键值对 (key=1, value="old") |
| **测试步骤** | 1. 插入键值对 (key=1, value="new") <br> 2. 调用 `btree_find(1)` |
| **预期结果** | 返回更新后的值 "new" |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-009: btree_first 游标

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-009 |
| **标题** | btree_first 游标 |
| **优先级** | P0 |
| **前置条件** | 已插入多个键值对 |
| **测试步骤** | 1. 调用 `btree_first()` 获取游标 <br> 2. 验证游标有效 |
| **预期结果** | 游标指向最小键 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-010: btree_last 游标

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-010 |
| **标题** | btree_last 游标 |
| **优先级** | P0 |
| **前置条件** | 已插入多个键值对 |
| **测试步骤** | 1. 调用 `btree_last()` 获取游标 <br> 2. 验证游标有效 |
| **预期结果** | 游标指向最大键 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-011: 游标向前迭代

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-011 |
| **标题** | 游标向前迭代 |
| **优先级** | P0 |
| **前置条件** | 已插入多个键值对 |
| **测试步骤** | 1. 获取第一个游标 <br> 2. 调用 `btree_cursor_next()` 移动 <br> 3. 调用 `btree_cursor_valid()` 检查 |
| **预期结果** | 游标正确移动到下一个键 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-012: 游标向后迭代

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-012 |
| **标题** | 游标向后迭代 |
| **优先级** | P0 |
| **前置条件** | 已插入多个键值对 |
| **测试步骤** | 1. 获取最后一个游标 <br> 2. 调用 `btree_cursor_prev()` 移动 <br> 3. 调用 `btree_cursor_valid()` 检查 |
| **预期结果** | 游标正确移动到上一个键 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-013: btree_cursor_valid 有效性检查

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-013 |
| **标题** | btree_cursor_valid 有效性检查 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `btree_first()` 获取游标 <br> 2. 迭代到末尾后调用 `btree_cursor_valid()` |
| **预期结果** | 有效时返回 true，无效时返回 false |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-014: btree_cursor_peek 预览

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-014 |
| **标题** | btree_cursor_peek 预览 |
| **优先级** | P1 |
| **前置条件** | 已插入多个键值对 |
| **测试步骤** | 1. 获取游标 <br> 2. 调用 `btree_cursor_peek()` 预览当前 <br> 3. 调用 `btree_cursor_valid()` 确认游标未移动 |
| **预期结果** | peek 不消耗游标位置 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-015: btree_get 消费并返回

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-015 |
| **标题** | btree_get 消费并返回 |
| **优先级** | P0 |
| **前置条件** | 已插入键值对 (key=1, value="test") |
| **测试步骤** | 1. 获取游标 <br> 2. 调用 `btree_get()` 获取当前键值 <br> 3. 再次调用 `btree_cursor_valid()` |
| **预期结果** | get 返回键值，游标已移动 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-016: 跨页遍历

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-016 |
| **标题** | 跨页遍历 |
| **优先级** | P0 |
| **前置条件** | 已插入大量数据触发多页面 |
| **测试步骤** | 1. 从第一页开始遍历 <br> 2. 通过 right_sibling 链跨页移动 |
| **预期结果** | 正确遍历所有页面数据 |
| **测试结果** | ✅ PASS |

---

### TC-BTREE-017: 多级 B+Tree

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-BTREE-017 |
| **标题** | 多级 B+Tree |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 插入大量数据触发多级树 <br> 2. 验证所有键值对可找到 |
| **预期结果** | 多级树正确构建，数据完整 |
| **测试结果** | ✅ PASS |

---

## 测试结果汇总

| 测试用例 | 结果 | 备注 |
|---------|------|------|
| TC-BTREE-001 | ✅ PASS | |
| TC-BTREE-002 | ✅ PASS | |
| TC-BTREE-003 | ✅ PASS | |
| TC-BTREE-004 | ✅ PASS | |
| TC-BTREE-005 | ✅ PASS | |
| TC-BTREE-006 | ✅ PASS | |
| TC-BTREE-007 | ✅ PASS | |
| TC-BTREE-008 | ✅ PASS | |
| TC-BTREE-009 | ✅ PASS | |
| TC-BTREE-010 | ✅ PASS | |
| TC-BTREE-011 | ✅ PASS | |
| TC-BTREE-012 | ✅ PASS | |
| TC-BTREE-013 | ✅ PASS | |
| TC-BTREE-014 | ✅ PASS | |
| TC-BTREE-015 | ✅ PASS | |
| TC-BTREE-016 | ✅ PASS | |
| TC-BTREE-017 | ✅ PASS | |

**通过率: 17/17 (100%)**