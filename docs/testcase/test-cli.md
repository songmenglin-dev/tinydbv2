# CLI 测试用例

## 测试范围

测试 `src/cli/cli.c` 的命令行界面功能。

## 测试用例

### TC-CLI-001: CLI 初始化和关闭

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-001 |
| **标题** | CLI 初始化和关闭 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `cli_init()` <br> 2. 调用 `cli_shutdown()` |
| **预期结果** | CLI 初始化成功，关闭后无资源泄漏 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-002: CLI 执行 SQL 语句

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-002 |
| **标题** | CLI 执行 SQL 语句 |
| **优先级** | P0 |
| **前置条件** | CLI 已初始化，服务器运行中 |
| **测试步骤** | 1. 调用 `cli_execute_sql("SELECT * FROM users")` |
| **预期结果** | 返回查询结果 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-003: CLI 连接和断开

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-003 |
| **标题** | CLI 连接和断开 |
| **优先级** | P0 |
| **前置条件** | 服务器运行中 |
| **测试步骤** | 1. 调用 `cli_connect()` <br> 2. 调用 `cli_disconnect()` |
| **预期结果** | 连接成功，断开正常 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-004: CLI 表格格式化输出

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-004 |
| **标题** | CLI 表格格式化输出 |
| **优先级** | P0 |
| **前置条件** | 有查询结果数据 |
| **测试步骤** | 1. 调用 `cli_format_table()` |
| **预期结果** | 输出带边框的表格格式 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-005: CLI 表格头部格式化

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-005 |
| **标题** | CLI 表格头部格式化 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `cli_format_header()` |
| **预期结果** | 输出带分隔线的表头 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-006: CLI 表格底部格式化

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-006 |
| **标题** | CLI 表格底部格式化 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `cli_format_footer(row_count, elapsed_sec)` |
| **预期结果** | 输出行数和执行时间 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-007: CLI 格式化行输出

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-007 |
| **标题** | CLI 格式化行输出 |
| **优先级** | P0 |
| **前置条件** | 有行数据 |
| **测试步骤** | 1. 调用 `cli_print_box_row()` |
| **预期结果** | 输出对齐的表格行 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-008: CLI 执行时间跟踪

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-008 |
| **标题** | CLI 执行时间跟踪 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `gettimeofday()` 记录开始时间 <br> 2. 执行查询 <br> 3. 记录结束时间 <br> 4. 计算差值 |
| **预期结果** | 正确计算执行时间（秒） |
| **测试结果** | ✅ PASS |

---

### TC-CLI-009: CLI 参数解析

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-009 |
| **标题** | CLI 参数解析 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 调用 `cli_parse_args()` 解析 `-c "SQL"` 参数 |
| **预期结果** | 正确提取 SQL 语句 |
| **测试结果** | ✅ PASS |

---

### TC-CLI-010: 连接失败处理

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CLI-010 |
| **标题** | 连接失败处理 |
| **优先级** | P1 |
| **前置条件** | 服务器未运行 |
| **测试步骤** | 1. 调用 `cli_connect()` 连接不存在的服务器 |
| **预期结果** | 返回连接错误 |
| **测试结果** | ✅ PASS |

---

## 测试结果汇总

| 测试用例 | 结果 | 备注 |
|---------|------|------|
| TC-CLI-001 | ✅ PASS | |
| TC-CLI-002 | ✅ PASS | |
| TC-CLI-003 | ✅ PASS | |
| TC-CLI-004 | ✅ PASS | |
| TC-CLI-005 | ✅ PASS | |
| TC-CLI-006 | ✅ PASS | |
| TC-CLI-007 | ✅ PASS | |
| TC-CLI-008 | ✅ PASS | |
| TC-CLI-009 | ✅ PASS | |
| TC-CLI-010 | ✅ PASS | |

**通过率: 10/10 (100%)**