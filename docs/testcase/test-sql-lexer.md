# SQL Lexer 测试用例

## 测试范围

测试 `src/sql/lexer.c` 的词法分析功能。

## 测试用例

### TC-LEX-001: 基本关键字识别

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-001 |
| **标题** | 基本关键字识别 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"SELECT * FROM users"` <br> 2. 获取 token 序列 |
| **预期结果** | `TOKEN_SELECT`, `TOKEN_STAR`, `TOKEN_FROM`, `TOKEN_IDENTIFIER("users")`, `TOKEN_SEMICOLON` |
| **测试结果** | ✅ PASS |

---

### TC-LEX-002: 整数 tokenization

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-002 |
| **标题** | 整数 tokenization |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"42"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_INTEGER` 类型，值为 42 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-003: 十六进制整数

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-003 |
| **标题** | 十六进制整数 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"0xFF"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_INTEGER` 类型，值为 255 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-004: 浮点数 tokenization

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-004 |
| **标题** | 浮点数 tokenization |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"3.14"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_REAL` 类型，值为 3.14 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-005: 科学计数法浮点数

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-005 |
| **标题** | 科学计数法浮点数 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"1.5e-3"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_REAL` 类型，值为 0.0015 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-006: 字符串 tokenization

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-006 |
| **标题** | 字符串 tokenization |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"'hello world'"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_STRING` 类型，值为 `"hello world"` |
| **测试结果** | ✅ PASS |

---

### TC-LEX-007: 转义字符串

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-007 |
| **标题** | 转义字符串 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"'it''s working'"` <br> 2. 获取第一个 token |
| **预期结果** | `TOKEN_STRING` 类型，值为 `"it's working"` |
| **测试结果** | ✅ PASS |

---

### TC-LEX-008: 操作符识别

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-008 |
| **标题** | 操作符识别 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"= != <> < > <= >="` <br> 2. 依次获取所有 token |
| **预期结果** | 所有比较操作符正确识别 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-009: 关键字大小写不敏感

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-009 |
| **标题** | 关键字大小写不敏感 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"select SELECT SeLeCt"` <br> 2. 获取所有 token |
| **预期结果** | 所有变体都识别为 `TOKEN_SELECT` |
| **测试结果** | ✅ PASS |

---

### TC-LEX-010: Peek token (向前查看)

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-010 |
| **标题** | Peek token (向前查看) |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"SELECT * FROM users"` <br> 2. 调用 peek 获取下一个 token <br> 3. 再调用 advance |
| **预期结果** | peek 不消耗 token，advance 后 token 被消耗 |
| **测试结果** | ✅ PASS |

---

### TC-LEX-011: Positional placeholders

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-LEX-011 |
| **标题** | Positional placeholders |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试步骤** | 1. 创建 lexer，传入 `"$1 $123"` <br> 2. 获取两个 token |
| **预期结果** | 正确识别位置占位符 $1 和 $123 |
| **测试结果** | ✅ PASS |

---

## 测试结果汇总

| 测试用例 | 结果 | 备注 |
|---------|------|------|
| TC-LEX-001 | ✅ PASS | |
| TC-LEX-002 | ✅ PASS | |
| TC-LEX-003 | ✅ PASS | |
| TC-LEX-004 | ✅ PASS | |
| TC-LEX-005 | ✅ PASS | |
| TC-LEX-006 | ✅ PASS | |
| TC-LEX-007 | ✅ PASS | |
| TC-LEX-008 | ✅ PASS | |
| TC-LEX-009 | ✅ PASS | |
| TC-LEX-010 | ✅ PASS | |
| TC-LEX-011 | ✅ PASS | |

**通过率: 11/11 (100%)**